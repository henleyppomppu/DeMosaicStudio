# DeMosaic Studio — 작업 인계 문서

Claude Code가 이 저장소에서 작업을 이어갈 때 **가장 먼저 읽는 문서**입니다.
저장소에 이미 있는 내용은 반복하지 않고 링크만 겁니다. 여기 적는 것은 **코드를 읽어서는 알 수
없는 것들** — 무엇이 실제로 검증됐고 무엇이 아닌지, 실기에서 무엇이 깨졌고 왜 그렇게 고쳤는지.

| 먼저 볼 곳 | 내용 |
| --- | --- |
| [`prd.md`](prd.md) | 단일 진실 공급원 (v3.3). 이 문서와 충돌하면 PRD가 이긴다 |
| [`prompt.md`](prompt.md) | 실행 프롬프트. 페이즈 분해와 에이전트 배치 |
| [`docs/DECISIONS.md`](docs/DECISIONS.md) | ADR 21건. **되돌리기 전에 반드시 확인** |
| [`docs/ERROR_CODES.md`](docs/ERROR_CODES.md) | 오류 코드 43개 |
| [`docs/WORKER_PROTOCOL.md`](docs/WORKER_PROTOCOL.md) | 프로토콜 v1.1 |
| [`docs/phase0-report.md`](docs/phase0-report.md) | **실현 가능성 게이트 — `PASS_ALIGNMENT_BLOCKED`** |
| [`docs/phase1-detector-report.md`](docs/phase1-detector-report.md) | **실험 A — 탐지기가 신호를 학습하는가: 예 (val IoU 0.82)** |
| [`docs/phase2-alignment-report.md`](docs/phase2-alignment-report.md) | **밀집 정렬 — 간극의 19%만 메움. 나머지는 정렬 문제가 아님** |
| [`docs/phase3-endtoend-report.md`](docs/phase3-endtoend-report.md) | **전 구간 + 어블레이션 래더 — 손상의 최대 원인은 RGB 왕복이었다** |

---

## 1. 지금 상태 (2026-08-22)

**Phase 0 완료 — 게이트 판정 `PASS_ALIGNMENT_BLOCKED`.** 0.5만 부분 완료.
**Phase 1 실험 A 완료 — 탐지기가 신호를 학습합니다 (held-out val IoU 0.820).**
**Phase 2 정렬 실험 완료 — 밀집 흐름이 간극의 19%만 메웁니다. 나머지는 정렬 문제가 아닙니다.**
**Phase 3 전 구간 최초 실행 — 파이프라인이 돌지만 출력이 입력보다 나쁩니다.**

### 게이트 결과 (`docs/phase0-report.md`, 576측정)

| | |
| --- | --- |
| 결정 대역(SCREEN, 블록 6~12) oracle 이득 | **+3.298 dB** |
| 같은 대역 estimated(전역 평행이동) 이득 | **−0.861 dB** |
| 노이즈 바닥 | **0.000 dB** (x264가 이 설정에서 결정론적 → 위 차이는 전부 실제) |
| OBJECT 앵커링 oracle 이득 | **−0.79 ~ −1.50 dB** (전 블록 크기에서 음수) |

**결론 세 줄.**
1. 정보는 이웃 프레임에 **있다.** §1.4의 전제는 성립하고 Phase 2는 죽지 않았다.
2. **정렬이 임계 경로다.** +3.30과 −0.86의 간극이 다중 프레임 아키텍처의 이득 전부다.
   복원 모델이 아니라 §5.7이 Phase 2의 첫 작업이다.
3. **객체 추종 격자는 융합하면 손해다.** 안 하느니만 못하다 — §5.4.4 앵커링 판정이
   진단용이 아니라 필수 안전장치다.

### 실제로 실행해서 확인된 것

```
dotnet build DeMosaicStudio.slnx -c Release   경고 0 / 오류 0
dotnet test  DeMosaicStudio.slnx -c Release   88 통과
.venv\Scripts\python.exe -m pytest            134 통과
scripts\check-environment.ps1                 7/7 PASS, exit 0
```

- **환경 구축 완료.** Python 3.12.10, GPL FFmpeg(x264/x265/NVENC), PyAV 14.0.1, numpy.
  `scripts/setup-worker.ps1`이 재현하고 두 번째 실행에서 아무것도 다시 받지 않는 것까지 확인.
- **교차 언어 패리티 양방향 검증.** 오류 코드 43개, 지문 정규 텍스트·다이제스트, `PROTOCOL_VERSION`.
- **task 0.4 패스스루 PoC.** CFR·VFR 타임라인 보존, 오디오 3트랙 비트 동일, 손상 파일 E2003.
- **클린 코퍼스.** Tears of Steel(CC BY 3.0) 24클립, 모션 실측 층화(static 5 / slow 8 /
  medium 7 / fast 4). 클립은 gitignore, 매니페스트+SOURCES.md만 추적 — URL·해시·빌드 명령으로 재현.

### 실험 A 결과 (`docs/phase1-detector-report.md`)

| | |
| --- | --- |
| held-out val IoU | **0.820** (train 0.869 — 과적합 거의 없음) |
| 블록 4~6px / 19~24px IoU | **0.748 / 0.878** — 작을수록 어렵다 |
| 클린 크롭이 면적 0.5% 초과 오탐 | **10.6%** (§5.2.5a는 0.5% 요구) |
| **학습 노이즈 바닥** | val IoU **±0.005**, p10 **±0.021**, FP율 **±1.2pp** (동일 설정 2회) |

**요지 두 줄.**
1. 아키텍처(D-03)는 96초 코퍼스로도 신호를 학습합니다. **지금 병목은 클린 영상 시간이 아닙니다.**
2. **거짓 양성이 병목입니다.** 그리고 실패는 **작은 블록(4~6px)** 에 몰려 있습니다 — 옅은 모자이크와
   평범한 압축 소프트닝이 거의 같은 신호라서. 복원은 정확히 반대로 큰 블록에서 어렵습니다.

### 정렬 실험 결과 (`docs/phase2-alignment-report.md`, 64측정)

| 팔 | single 대비 |
| --- | ---: |
| 전역 평행이동 (게이트 수치) | −2.110 dB |
| **밀집 흐름 K=5** | **−0.791 dB** |
| **밀집 흐름 K=3** | **−0.443 dB** |
| oracle (완벽한 정렬) | +3.494 dB |

정렬 자체는 크게 좋아졌습니다 — 잔차 12.54 → 5.77(전역) → **3.91**(밀집). 그런데 이득은 여전히 음수.

**모션 대역이 답을 줍니다.**

| 대역 | oracle | 밀집 K=3 | 정렬 후 잔차 |
| --- | ---: | ---: | ---: |
| fast | +3.70 | −1.48 | 7.01 |
| medium | +4.40 | −0.64 | 3.95 |
| **slow** | +3.22 | **+0.39** | 3.43 |
| static | +2.66 | −0.04 | **1.27** |

**`static`은 정렬이 제일 좋은데(잔차 1.27) 이득이 없습니다.** 어긋난 게 없고 새 정보가 없는 겁니다 —
정지 피사체는 매 프레임 같은 블록에 들어갑니다. 그리고 oracle이 그 정지 클립에서 +2.66을 내는 건
**실제 영상에 없는 위상 다양성을 합성으로 만들어냈기 때문**입니다. oracle은 낙관적 상한 정도가
아니라 정지 콘텐츠에서는 존재할 수 없는 것을 재고 있습니다.

**`slow`만 이득이 나고, 게이트와 이 실험이 독립적으로 일치합니다**(+0.32 / +0.36).
그리고 **K=3이 모든 대역에서 K=5 이상**입니다 — 한계가 흐름 정확도가 아니라 **내용 대응**이라는 뜻.

### 전 구간 실행 결과 (`docs/phase3-endtoend-report.md`)

실제 프로토콜로 워커를 구동해 96프레임 영상 한 편을 처리했습니다. **프레임 수 96/96, PTS 오차 0틱**
— §5.1.7 불변식이 복원을 켠 상태로 완주했습니다. 다중 프레임 복원이 743회 실행됐습니다.

**그런데 출력이 입력보다 나쁩니다.**

| | 모자이크 입력 | 복원 출력 | 차이 |
| --- | ---: | ---: | ---: |
| 영역 내부 PSNR | 37.24 dB | 34.88 dB | **−2.36** |
| **영역 외부 PSNR** | 49.55 dB | 39.49 dB | **−10.06** |

개선된 프레임: **0/96**.

원인은 셋이고 전부 이미 측정된 것들입니다.
1. **탐지기 과다 발화.** 96프레임에 843영역 — 입력에는 하나뿐인데 프레임당 9개꼴.
   실험 A의 10.6% FP율이 영상으로 나타난 것입니다. 멀쩡한 곳을 "복원"하면 망가집니다.
2. **인코딩이 투명하지 않습니다.** 널런 측정: x265 CRF 20은 45.4dB/0.9864 — §5.1.8의 SSIM 0.99를
   못 넘깁니다. CRF 12는 통과. 기본값을 측정으로 정해야 합니다.
3. **블렌딩이 설계대로 8px 번집니다**(§5.11 팽창+페더). 마스크가 맞을 때는 옳지만 거짓 양성
   주변에서는 그냥 추가 손상입니다.

### 탐지기 v0.2.0 — 네거티브를 넓혀 재학습

전 구간 실행이 최상위 블로커를 탐지기 거짓 양성으로 지목해서, 먼저 **임계값을 실측**했습니다
(`scripts/calibrate_detector.py`, 정답 마스크가 있는 영상 위에서).

**어떤 임계값도 §5.2.5a를 못 맞춥니다.** v0.1.0 최선은 0.99에서 클린 프레임 18.8% 발화 — 기준
0.5%의 37배. 임계값 튜닝이 아니라 모델 문제라는 뜻이고, 그래서 재학습했습니다.

네거티브 비율 30%→**50%**, 그중 하드 네거티브 50%→**70%**, 클래스 3종→**6종**(디포커스, 진짜
주기 구조, 약한 재압축 추가). 학습 평가에 **`clean_frames_firing`**(프레임 단위, §5.2.5a가
실제로 요구하는 것)을 추가했습니다 — v0.1.0에 쓰던 크롭 면적 대리 지표는 10.6%로 편안했는데
실제 값은 기준의 37배였습니다. **그렇게 편안한 대리 지표는 없느니만 못합니다.**

| | v0.1.0 | **v0.2.0** |
| --- | ---: | ---: |
| val IoU | 0.820 | **0.788** |
| **영상에서 클린 프레임 발화 @0.99** | 18.8% | **9.4%** |

IoU 0.032 하락은 노이즈 바닥 0.005 밖이라 실제 비용입니다. **그래도 할 만한 거래였습니다** —
멀쩡한 영상을 망가뜨리는 실패를 절반으로 줄였으니까. **다만 여전히 기준의 19배**이고,
**만들 수 있는 네거티브는 이제 다 썼습니다.** 남은 건 §11.4가 수집해야 한다고 적은 절반입니다.

주의: `clean_frames_firing`은 **아무것도 예측 안 하는 모델도 통과**시킵니다(300스텝 스모크에서
IoU 0인 채로 0.39%). IoU와 함께 읽어야 합니다.

### 어블레이션 래더 — 한 번에 한 변수 (`scripts/eval_endtoend.py`)

거짓 양성을 4.6배 줄였는데 **품질이 거의 안 움직였습니다.** 그래서 손상의 주원인을 따로 찾았고,
그게 후보 셋 중 어디에도 없던 것이었습니다.

출력을 **입력과** 비교하고 복원한 적 없는 픽셀로 한정하니:

| | 미복원 픽셀에서 입력 대비 PSNR |
| --- | ---: |
| 인코딩만 CRF 20 | 46.46 |
| **파이프라인 CRF 20** | **41.29** |

처리 없이 왕복만 재보니 원인이 나왔습니다:

| 한 프레임 왕복 | 휘도 PSNR |
| --- | ---: |
| `rgb24` 경유 (파이프라인이 하던 것) | **45.33 dB** |
| `yuv420p` 평면 | **무손실** |

**RGB 왕복이 인코더보다 비쌌고, 파이프라인이 건드린 적 없는 픽셀 포함 프레임 전체에 적용됐습니다.**
§5.1.8이 막으려는 바로 그것입니다. 이제 휘도 평면만 다루고 크로마는 디코딩된 그대로 둡니다 —
모자이크는 휘도 디테일을 파괴하는 것이고, 색에 대해선 파이프라인에 근거가 없습니다.
`astype(uint8)` 절삭도 반올림으로 바꿨습니다(프레임 전체가 반 레벨씩 아래로 편향).

| 단계 | 내부 | 입력 대비 | 외부 | 입력 대비 | 영역 |
| --- | ---: | ---: | ---: | ---: | ---: |
| (입력) | 37.24 | — | 49.55 | — | — |
| 베이스라인 | 36.30 | **−0.94** | 43.51 | **−6.05** | 823 |
| + v0.2.0 | 36.30 | −0.94 | 43.50 | −6.05 | 521 |
| + 임계값 0.9 | 36.45 | −0.80 | 43.94 | −5.62 | 177 |
| + CRF 12 | 36.55 | **−0.69** | 44.98 | **−4.57** | 177 |

내부 −2.15 → **−0.69**, 외부 −9.12 → **−4.57**. **여전히 입력보다 나쁘지만**, "접근이 틀렸다"가
아니라 "메울 수 있어 보이는" 크기가 됐습니다.

**기여도 분해가 준 교훈:** 탐지기는 최상위 블로커로 **정확히 지목됐지만**, 손상의 원인으로는
**잘못 지목됐습니다.** 4.6배 줄여서 얻은 게 0.15dB(내부)입니다. 결함을 찾는 것과 원인을 찾는 것은
다른 일이고, 래더가 그걸 갈라줍니다.

### 전 구간 실행이 잡은 결함 3건 — 단위 테스트로는 절대 안 잡히는 것들

1. **전체 프레임에서 복원하고 있었습니다.** VRAM 10.9GB, 8분에 프레임 몇 개. §5.5 ROI 스코핑은
   최적화가 아니라 **파이프라인이 완주하느냐 마느냐**입니다. → `roi.py` + 테스트 16개
2. **RAFT는 128px 미만에서 실패합니다.** 96×96 실패, 128×128 성공(실측). 작은 ROI가 이 파이프라인의
   **일반적인** 경우라서 모든 정렬이 실패하고 라우터가 조용히 단일 프레임으로 떨어졌습니다 —
   로그는 윈도우 3이라고 말하면서. → 정렬기 내부에서 패딩
3. **`jobId`가 봉투에 있는데 payload에서 찾고 있었습니다.** 모든 `process`가 E7006으로 거부됐습니다.
4. **마스크 임계값이 0.5로 하드코딩**돼 설정을 무시했습니다. 교정한 운영점을 적용할 방법이
   아예 없었습니다. §5.2.3은 "탐지 신뢰도"와 "마스크 이진화 임계값"을 별개로 정의합니다.
5. **콘솔에 em dash를 써서 cp949에서 죽었습니다** — 제가 §5.1에 적어둔 그 함정입니다.
   측정은 이미 JSON에 쓰인 뒤라 데이터는 살았지만, 두 번 밟았습니다.

### 마스크 밖 손실의 분해 (`docs/untouched-region-report.md`)

−4.57 dB가 전부 재인코딩 비용이라는 읽기는 **64%만 맞았고, 그 처방의 상한은 0**이었습니다.

| | dB | 몫 |
| --- | ---: | ---: |
| 인코더 (input → null run) | 2.94 | 64% |
| 파이프라인 (null run → pipeline) | 1.63 | 36% |

- **인코더는 이미 R-1.8b의 투명성 기준을 넘습니다**(원본 대비 49.72 dB, 기준 42 dB). 2.94 dB는
  제3의 기준(clean)을 들이댔을 때 "투명"이 치르는 값입니다. 되사려면 제품이 스스로 고른 기준을
  넘어서야 합니다.
- **스마트컷(R-1.8c)은 이 파일에서 아무것도 못 아낍니다.** 탐지기가 96/96 프레임에서 발화하고
  GOP은 2개뿐이라 복사 가능한 프레임이 0입니다. 원리상 옳지만 **가치는 오탐률이 정합니다.**
- **남은 1.63 dB는 번짐이 아니라 오탐입니다.** 파이프라인이 모자이크 밖 픽셀의 **5.84%**를
  건드려 **−4.21 dB** 악화시킵니다. 8 px 헤일로로 설명되는 면적(약 0.43%)의 13배입니다.
- 즉 **복원은 지금 닿는 곳마다 악화시킵니다** — 안에서 −0.69 dB, 밖에서 −4.21 dB.

### 게이트 교정이 복원기의 버그를 찾아냈다 (`docs/gate-and-observation-report.md`)

`minRegionConfidence`를 교정하려면 영역별 정답이 필요했고, 그 정답이 게이트로는 못 고칠 것을 말했습니다.

**교정 결과(수정 전).** 214건 중 **20건만 개선**. 무게 −1.48 dB, 오라클 +0.018 dB.
진짜 모자이크 위 −1.20 dB, 클린 화면 위 −2.98 dB. **모든 경로가 음수**라 고를 좋은 쪽이 없었습니다.

**단일 프레임이 −2.12 dB인 게 앞뒤가 안 맞았습니다** — 정렬이 필요 없는 경로니까요. 원시연산을
직접 찔러보니 **단일 관측 역투영은 구조적으로 무연산**입니다(잔차가 블록 안에서 평균 0). 격자를
일부러 틀려도 +0.00 dB입니다. 솔버는 결백했습니다.

**진짜 원인:** 파이프라인이 `Observation(block_average(crop_target, ...))`를 넘겼습니다 —
**이미 관측인 것에 순방향 연산자를 재적용**한 것입니다. 그 결과 ① 모자이크가 *추정된* 격자로
재양자화되어 블록이 어긋나고, ② ROI 사각형 안의 마스크 **바깥 깨끗한 화면**이 뭉개진 뒤
팽창·페더로 다시 합성됐습니다. 다중 프레임은 이웃 크롭에도 같은 짓을 했습니다.

| | 수정 전 | 수정 후 |
| --- | ---: | ---: |
| 파이프라인의 마스크 밖 손실 몫 | 1.63 dB | **0.26 dB** |
| 건드린 바깥 픽셀의 손상 | −4.21 dB | **−0.52 dB** |
| 개선한 복원 | 214건 중 20 | **208건 중 68** |

**그리고 게이트가 그걸 표현하지 못했습니다.** 교정된 0.88을 넣으니 **아무것도 복원되지
않았습니다.** 결함 셋이 한꺼번에 나왔습니다 — 해제가 `임계값 + 여백`을 요구해 confidence 상한
(10px면 0.90) 때문에 **0.85 위는 도달 불가**, 트랙이 **열린 채로 시작**해 매 트랙 2프레임이
무조건 복원, 그리고 파라미터 이름이 `smoothed_confidence`인데 **원시값을 넘기고** 있었습니다.
장기 트랙 하나(208건 중 95건)가 좋은 프레임에 열린 뒤 나쁜 프레임을 타고 넘었습니다.

| 게이트에 넣은 신호 | 최적 임계값 | 무게 dB | 유지 |
| --- | ---: | ---: | ---: |
| 원시 프레임별 | 0.91 | 0.0000 (전부 보류) | 0 |
| **평활, α = 1/3 (게이트 자신의 창)** | **0.88** | **+0.0511** | 18 |
| 영역별 이상값 (히스테리시스 없음, 깜빡임) | 0.88 | +0.0751 | 47 |

**전 구간 결과.**

| arm | 영역 안 PSNR | 입력 대비 | 개선 프레임 |
| --- | ---: | ---: | ---: |
| 모자이크 입력 (기준) | 32.854 | — | — |
| 수정 전 | 32.095 | −0.759 | 4% |
| 수정 후, 게이트 없음 | 32.469 | −0.385 | 42% |
| **수정 후, 게이트 0.88** | **32.872** | **+0.018** | **51%** |

**+0.018 dB는 복원이 아니라 손익분기입니다.** 프레임을 눈으로 봤고 같은 말을 합니다 —
모자이크는 여전히 그대로이고 블록 경계만 약간 부드럽습니다. 바뀐 것은 파이프라인이 화면을
**손상시키기를 멈췄다**는 것뿐입니다.

**기본값은 0.0(끔)으로 둡니다.** 0.87이 −0.60 dB, 0.88이 +0.05 dB — 합성 클립 하나 위의
칼날입니다. 기록할 이유는 되어도 출시할 이유는 안 됩니다.

### 그 과정에서 고친 결함 3건 — 전부 "요약과 바이트가 어긋나는" 종류

**잡 러너에는 테스트가 하나도 없었습니다.** 그래서 셋 다 통과하고 있었습니다.

1. **R-1.8a는 보고만 되고 구현이 없었습니다.** `passthrough: regionsDetected == 0`은 *결정*에
   대한 주장이었고 영상은 어느 쪽이든 전부 재인코딩됐습니다. 이제 진짜 스트림 카피이고
   `T-IO-PASSTHROUGH-COPY-01`이 비디오 스트림 바이트 동일을 검사합니다. 실기 확인: 클린 클립이
   8,240,353바이트·패킷 해시 동일로 나왔습니다.
2. **`analyze`가 복원을 전부 돌리고 픽셀만 버렸습니다** — 162초 대 본 작업 153초 — 게다가 출력
   경로가 없으면 **원본 옆에 파일을 썼습니다.** 이제 탐지·추적만 하고 아무것도 쓰지 않으며
   `sampleEvery`를 지킵니다. **162초 → 35초**, `--sample-every 4`면 11초, 탐지 결과는 동일.
3. **PyAV는 비디오 리먹스를 아예 못 합니다.** `add_stream_from_template`이 인코더 스트림을
   만들어 패킷당 1바이트를 씁니다(23KB → 21바이트, 디코드 불가). 오디오는 같은 호출로 멀쩡합니다.
   그래서 `run_stream_copy`는 ffmpeg로 나갑니다 — D-22.

### 존재하지 않는 것

WPF 앱, C# Application/Infrastructure 계층, 체크포인트/재개, 미리보기. **네거티브 코퍼스 없음**(이게 다음 병목).

**지금 파이프라인을 돌리는 방법은 `scripts/run_job.py` 하나뿐입니다.** WPF 호스트가 쓸 것과
같은 프로토콜을 같은 stdio 전송으로 말하는 **클라이언트**입니다 — 러너를 직접 import하는
편법이 아니라 워커를 자식 프로세스로 띄웁니다. 그래서 여기서 되는 것은 호스트에서도 됩니다.

```powershell
.\.venv\Scripts\python.exe scripts\run_job.py INPUT.mp4 OUTPUT.mp4
.\.venv\Scripts\python.exe scripts\run_job.py INPUT.mp4 --probe-only
```

기본값은 측정으로 정해진 것들입니다 — `--threshold 0.9`(교정된 동작점), `--crf 12`(투명하다고
측정된 지점). **실측 처리량은 0.63 fps로 §6.1의 MVP 목표 ≥4 fps에 한참 못 미칩니다.**
탐지기 체크포인트 `docs/phase1-detector.pt`는 **실험 산출물이지 모델 릴리스가 아닙니다** —
`metadata.json`도 버전도 §14.1 모델 스토어 등록도 없습니다.

열화 생성기는 §11.3 랜덤화 매트릭스(CRF 사다리 재압축·크로마 서브샘플링·모션 블러·다중 ROI)가
미완입니다.

### 게이트가 측정하지 **않은** 것

- **LPIPS와 워핑 오차.** §1.4.3은 PSNR·LPIPS·워핑을 함께 보라고 하는데 셋 중 하나만 쟀습니다.
  LPIPS는 사전학습 네트워크가 필요해 Phase 2에 옵니다. **그때 게이트를 다시 돌리세요.**
- 학습 모델, GPU 경로, 실제 모자이크 영상.
- oracle 팔은 **후한 상한**입니다 — 이웃을 타깃에서 합성하므로 내용이 완전히 동일합니다.
  실제 이웃에는 가림·독립 물체 운동·조명 변화가 있어 어떤 정렬로도 증거가 되지 않습니다.

## 2. 검증 명령

```powershell
dotnet build DeMosaicStudio.slnx -c Release   # 경고 0 유지
dotnet test  DeMosaicStudio.slnx -c Release   # 현재 108/108
.\.venv\Scripts\python.exe -m pytest        # 현재 336 통과 · 2 스킵 (약 60초)
.\scripts\check-environment.ps1              # 현재 7/7
```

솔루션 파일은 `.slnx`입니다(.NET 10 기본). `dotnet build DeMosaicStudio.sln`은 파일이 없다고 죽습니다.
`python`은 Microsoft Store 스텁이라 **반드시 `.venv\Scripts\python.exe`를 쓰세요.**
미디어 픽스처를 다시 만들려면 `.venv\Scripts\python.exe scripts\make_fixtures.py` — 단 §4.10을 먼저 읽으세요.

**"통과"를 "검증됨"으로 착각하지 마세요.** 444개(C# 108 + Python 336) 중 실제 미디어·GPU를
건드리는 것은 일부뿐이고, **실제 모자이크 영상은 아직 한 번도 시험되지 않았습니다.**

게이트를 다시 돌리려면(약 20분):
`.\.venv\Scripts\python.exe scripts\eval_multiframe_gate.py --clips 12 --targets 3`

---

## 3. 실기 환경 (2026-08-22 실측)

| 항목 | 값 |
| --- | --- |
| PC | Windows 11 Pro, 한국어 로캘(**CP949**), **PowerShell 5.1** |
| GPU | RTX 3080 Ti 12GB, 드라이버 591.86 |
| .NET SDK | 10.0.302 (**유일**. .NET 8 없음 → D-02의 근거) |
| Python | 3.12.10 (`.venv`). **`python`은 Store 스텁이므로 `.venv\Scripts\python.exe`를 쓰세요** |
| FFmpeg | `tools/ffmpeg` — GPL, x264/x265/NVENC |
| 소스 | `D:\Workspace\DeMosaicStudio` |

`CP949 + PowerShell 5.1`이 아래 함정들의 원인입니다. `pwsh` 7로 검증하면 전부 통과하는데
실기에서 깨집니다.

---

## 4. 이번 세션에서 실제로 밟은 것

기록으로 남깁니다. 전부 다시 밟기 쉬운 것들입니다.

1. **`dotnet new sln`이 `.slnx`를 만듭니다** (.NET 10). `DeMosaicStudio.sln`으로 빌드하면
   `MSB1009: 프로젝트 파일이 없습니다`가 납니다. 이 문서와 `prompt.md`의 명령을 `.slnx`로
   맞춰 뒀습니다.
2. **`CA1720`로 빌드가 깨졌습니다.** `enum GridAnchor { Screen, Object, Unknown }`의 `Object`가
   형식 이름과 겹칩니다. `TreatWarningsAsErrors=true`라 경고가 아니라 오류입니다.
   → `ObjectTracked`로 개명. 프로토콜 와이어 값은 여전히 `OBJECT`입니다.
3. **`.ps1`의 `§` 문자가 콘솔에서 깨집니다.** 파일은 UTF-8 BOM이라 **파싱은 정상**인데,
   `Write-Host`가 CP949 콘솔로 출력하면서 `§4.5`가 `��4.5`로 나옵니다. 파일 인코딩 문제가
   아니라 출력 인코딩 문제입니다. → 콘솔에 찍는 문자열은 ASCII만 쓰도록 바꿨습니다
   (`prd.md section 4.5`). 주석·문서 문자열의 `§`는 그대로 둬도 됩니다.
4. **테스트 프로젝트에 `CA1707`이 걸립니다.** 테스트 메서드 이름의 밑줄 때문입니다.
   → `tests/Directory.Build.props`에서 억제. 이 파일은 루트 `Directory.Build.props`를
   `GetPathOfFileAbove`로 명시적으로 import해야 합니다 — 안 그러면 루트 설정이 통째로 무시됩니다.
5. **`worker/tests/__init__.py`와 `training/tests/__init__.py`가 충돌합니다.** 둘 다 `tests`
   라는 이름의 패키지가 되어 `ModuleNotFoundError: No module named 'tests.test_mosaic'`가 납니다.
   → 양쪽 `__init__.py`를 지웠습니다. pytest가 rootdir 기준으로 모듈명을 만들면 파일명만 겹치지
   않으면 됩니다.
6. **`av` 컨테이너의 `seek(0)`은 되감기가 아닙니다.** 가장 가까운 키프레임으로 이동하면서
   코덱 프리롤(AAC priming 패킷)을 흘립니다. 오디오 스트림 해시를 seek 후에 계산했더니 원본과
   출력이 달라 보였고, **실제로는 비트 동일**이었습니다. → 스트림마다 컨테이너를 새로 여세요.
7. **PyAV는 자체 FFmpeg를 번들하는데 NVENC가 없습니다.** `libx264`/`libx265`는 있고
   `h264_nvenc`/`hevc_nvenc`는 없습니다. Quality 프로파일(x265)은 in-process로 되지만
   **Speed 프로파일(NVENC)은 `tools/ffmpeg`를 서브프로세스로 불러야 합니다.** D-08과 §5.1.4의
   전제와 다릅니다.
8. **파이썬 `sys.stdout.encoding`이 `cp949`입니다.** 프로토콜은 UTF-8 JSON Lines라 그대로 두면
   비ASCII 로그 한 줄에 `UnicodeEncodeError`가 납니다. → `demosaic_worker/stdio.py`의
   `configure_stdio_utf8()`를 핸드셰이크 전에 호출하세요. 가드: `test_stdio.py`가 실제
   서브프로세스로 한글·일본어를 왕복시킵니다. **UTF-8 로캘 CI에서는 절대 안 잡히는 종류입니다.**
9. **`--help`가 CP949 콘솔에서 죽습니다.** argparse는 모듈 docstring을 `description=__doc__`로
   그대로 출력하므로, docstring의 `§`나 em dash가 `UnicodeEncodeError`가 됩니다.
   위 3번이 `.ps1`에서 겪은 것과 **같은 함정이 파이썬 쪽에서 세 번 재발**했습니다 —
   `eval_endtoend.py`에서 두 번(측정값은 JSON을 먼저 써 둔 덕에 살아남았습니다),
   `run_job.py --help`에서 한 번. 파일 자체는 멀쩡하고 **출력만** 깨지므로 문법 검사·import
   테스트로는 어느 것도 안 잡힙니다. → §5.7의 가드를 넣었고, 그 가드가 **즉시 6개를 더
   찾아냈습니다**(스크립트 전부의 `--help`가 이 기계에서 죽고 있었습니다).
10. **`make_fixtures.py`·`make_policy_fixture.py`에는 argparse가 없습니다.** 인자를 무시하고
   본문을 실행하므로 `--help`를 붙여도 **커밋된 픽스처를 재생성합니다.** 실제로 그렇게
   덮어썼습니다. mp4와 패리티 JSON은 **바이트 동일**하게 재생성됐지만 `multi_audio.mkv`만
   92바이트가 달라집니다 — 헤더의 Matroska SegmentUID/날짜라 내용은 같습니다. 즉
   **mkv 먹싱만 비결정적**입니다. 이 diff가 보이면 내용 변경이 아니니 `git checkout --`으로
   되돌리세요.

---

## 5. 반드시 지켜야 할 함정

각 항목에 가드 테스트가 있습니다. **지우거나 완화하지 마세요.**

### 5.1 `.ps1`·`.iss`는 UTF-8 **BOM**으로 저장
PowerShell 5.1은 BOM이 없으면 ANSI(CP949)로 읽습니다. CP949 선행 바이트가 다음 1바이트를
삼키는데 UTF-8 한글은 3바이트라 정렬이 밀리고, 결국 **실행이 아니라 파싱에서** 실패합니다.
`pwsh` 7은 BOM 없는 UTF-8을 잘 읽으므로 문법 검사로는 절대 안 잡힙니다.
→ 가드: `ScriptEncodingTests` (바이트 직접 검사, 글로브라 새 스크립트 자동 포함)

### 5.7 콘솔에 찍는 문자열은 ASCII만
이 기계의 콘솔은 CP949입니다. `print()`에 닿는 문자열과, **argparse가 `--help`로 출력하는
모듈 docstring**은 `§`·em dash를 쓰면 `UnicodeEncodeError`로 죽습니다. 출력에 닿지 않는
주석과 docstring은 무엇을 써도 됩니다 — 규칙은 **stdout에 도달하는 것**에만 적용됩니다.
→ 가드: `worker/tests/test_console_encoding.py`. AST를 걸어 `print()`/`write()`에 들어가는
문자열 리터럴만 검사하므로(주석은 봐주므로) 꺼야 할 이유가 생기지 않습니다. 여기에
`run_job.py --help`를 실제로 `PYTHONIOENCODING=cp949`로 실행하는 왕복 테스트가 붙어 있습니다.

### 5.2 파이프라인 결과는 `@()`로 **전체**를 감쌀 것
```powershell
$x = @($obj.items) | Where-Object { $_ }    # 틀림 — 비면 $null, $null.Count 는 오류
$x = @(@($obj.items) | Where-Object { $_ }) # 맞음
```
`check-environment.ps1`이 이 규칙을 지키고 있습니다. 손댈 때 깨뜨리지 마세요.

### 5.3 지문에 키를 넣고 빼는 것은 항상 전체 캐시 무효화
`prd.md` §9.3. 지문 비교가 dict 상등이라 키 하나만 늘어도 기존 체크포인트가 전부 불일치가
됩니다. 지금은 체크포인트가 존재하지 않으므로 공짜지만, Phase 3 이후로는 아닙니다.
→ 가드: `SettingsFingerprintTests.Every_fingerprinted_setting_appears_in_its_canonical_form`

### 5.4 성능 손잡이는 지문에서 제외
`computeType`/precision은 OOM 사다리가 **실행 중에** 바꿉니다. 지문에 넣으면 다운그레이드
이후 모든 이어하기가 "설정 바뀜"이 되어 방금 끝낸 작업을 다시 돌립니다.
→ 가드: `Performance_knobs_discard_nothing`

### 5.5 C#/Python 패리티
오류 코드와 설정 지문은 두 언어 구현이 갈라지지 않도록 `fixtures/parity/`로 묶여 있습니다.
한쪽만 고치면 테스트가 잡습니다. **양방향으로 실제 검증됐습니다** — 지문 정규 텍스트·다이제스트,
오류 코드 43개, `PROTOCOL_VERSION` 전부.

### 5.6 불변 규칙
한 프레임은 **완전 복원이거나 원본과 바이트 동일**이거나 둘 중 하나입니다. 세 번째 상태는
없습니다 (`prd.md` §5.8.2). 부분 합성은 금지입니다.

---

## 6. 다음 작업 — 순서 고정

래더(§1)가 우선순위를 다시 바꿨습니다. **탐지기는 결함으로는 옳게 지목됐지만 손상의 원인으로는
틀리게 지목됐습니다.** 4.6배를 줄이고도 품질은 0.15 dB밖에 안 움직였고, 진짜 원인은 아무도
의심하지 않던 RGB 왕복이었습니다. 그러니 **다음에 무엇을 고치든 먼저 래더를 돌려 그것이
실제로 얼마를 차지하는지 재세요.**

1. **복원이 실제로 무언가를 복원하게 만들기.** 이제 이게 유일한 남은 질문입니다.
   파이프라인은 손상을 멈췄지만(−0.759 → +0.018 dB) 모자이크를 **제거하지는 못합니다** —
   출력 프레임을 보면 블록이 그대로 있습니다. 가장 유력한 다음 수는 **순방향 모델을 마스크
   인식형으로** 만드는 것입니다(D-24 마지막 항목): 지금 모델은 크롭 전체가 블록 평균됐다고
   가정하는데 실제로는 마스크 안만 그렇습니다. **이웃 프레임은 타깃에서 뭉개진 내용을 깨끗하게
   관측하고 있고, 그게 다중 프레임이 통할 수 있는 유일한 이유인데**, 지금은 그 둘을 구분하는
   것이 아무것도 없습니다.
2. **게이트 교정을 두 번째 콘텐츠에서 재현하기.** 0.87이 −0.60, 0.88이 +0.05 dB인 칼날은
   합성 클립 하나 위의 값입니다. 기본값을 바꾸려면 다른 콘텐츠에서 같은 모양이 나와야 합니다.
3. **미디어 계층 룩어헤드.** 지금 창은 §5.6이 규정한 중앙 정렬이 아니라 **인과적(causal)**
   입니다(D-19) — 인코더에 프레임을 만드는 즉시 넘기므로 미래 프레임이 없기 때문입니다.
   §2 정렬 실험은 **짧은 기저선이 더 잘 정렬된다**고 측정했는데, 인과 창은 기저선을 두 배로
   늘립니다. 룩어헤드가 §5.6을 실제로 사용 가능하게 만드는 유일한 방법입니다.
4. **전 구간 품질 검사를 테스트로.** 지금은 `scripts/eval_endtoend.py`라는 손으로 돌리는
   스크립트입니다. 회귀를 잡아야 할 숫자가 CI 밖에 있습니다.
5. **수집해야만 하는 네거티브.** §11.4의 절반(저비트레이트 블로킹·리샘플링·그레인)은 합성으로
   만들었고 **그 절반은 이미 다 썼습니다** — v0.2.0이 영상 단위 오발 18.8%→9.4%로 줄인 대가가
   IoU 0.032였습니다. 남은 것은 만들 수 없는 것들입니다: 진짜 디포커스/보케, 픽셀아트,
   LED월, 메시 원단.
6. **두 번째 CC 소스.** 지금 수치는 전부 영화 한 편이라 콘텐츠 간 일반화를 보지 못합니다.
7. **처리량.** 실측 0.63 fps 대 §6.1의 MVP 목표 ≥4 fps. **6배 이상 모자랍니다.** 품질이
   먼저지만, 이 격차는 설계 문제이지 튜닝 문제일 가능성이 높으므로 기록해 둡니다.
8. **학습된 복원기 — 여전히 가장 중요한 미해결 질문.** 지금까지의 수치는 전부 **고전
   해법(IBP)** 입니다. IBP는 정렬된 픽셀을 전부 증거로 취급합니다. 학습된 복원기는 대응이 나쁜
   증거를 무시하도록 학습될 수 있고, 그게 되는지가 다중 프레임이 slow 대역 밖에서도 살아남는지를
   결정합니다.
9. **LPIPS가 생기면 게이트를 다시 돌리세요.** §1.4.3은 PSNR·LPIPS·워핑을 함께 보라고 하는데
   셋 중 하나만 쟀습니다.
10. 곁들여: `Speed` 인코더 프로파일이 `tools/ffmpeg`를 서브프로세스로 불러야 합니다 (§4 발견 7).
   WPF 앱과 C# Application/Infrastructure 계층은 여전히 없습니다 (Phase 4).

---

## 7. 작업 방식 메모

- 커밋 메시지는 한국어, 무엇을 왜 고쳤는지 서술형으로.
- 사용자가 요청하지 않으면 커밋·푸시하지 않습니다. **현재 저장소는 `git init`만 된 상태이고
  커밋이 하나도 없습니다.**
- 파이썬 스크립트에 Windows 경로를 쓸 때는 raw 문자열(`r"C:\..."`)을 쓰세요. `SyntaxWarning:
  invalid escape sequence`가 계속 납니다.
- 순수 로직은 `Domain`(C#) / 순수 모듈(Python)에 둡니다. GPU나 파일 시스템 없이 테스트할 수
  없는 규칙은 잘못된 위치에 있는 것입니다 (`prd.md` §13.2).
- 측정하지 않은 수치를 문서·커밋 메시지·보고에 쓰지 마세요. 개선을 주장하려면 **노이즈 바닥을
  먼저 재세요** (§13.5).
