# DeMosaic Studio — 구현 실행 프롬프트 (goal 명령용)

이 문서 자체가 `goal` 명령에 넘길 프롬프트입니다. 아래 "실행 방법"대로 띄우면
메인 에이전트는 **Fable 5**, 하위 작업 에이전트는 전부 **Opus 5**로 돌아갑니다.

---

## 실행 방법

작업 디렉터리를 `D:\Workspace\DeMosaicStudio`로 두고, 메인 모델을 Fable 5로 세션을 시작합니다.

```bash
claude --model claude-fable-5
```

세션 안에서:

```text
goal @prompt.md
```

`goal` 명령이 없는 환경이면 이 파일의 "=== GOAL ===" 아래 전체를 그대로 붙여넣어도 동일하게
동작합니다. 프롬프트 자체가 자립적이라 슬래시 명령에 의존하지 않습니다.

**모델 배치는 프롬프트 본문의 §2에 규칙으로 박아 두었습니다.** 메인 세션이 Fable 5인 것은 위
`--model`이 결정하고, 서브 에이전트가 Opus 5인 것은 §2의 규칙에 따라 메인 에이전트가 `Agent`
툴을 호출할 때마다 `model: "opus"`를 명시해서 보장됩니다. 둘 중 하나라도 빠지면 배치가 깨지므로
§2를 지우지 마세요.

---

=== GOAL ===

# GOAL: DeMosaic Studio를 `prd.md` v3.0에 따라 구현한다

너는 이 저장소의 **총괄 에이전트**다. 직접 코드를 많이 쓰는 것이 아니라, 계획을 세우고, 작업을
하위 에이전트에 분배하고, 돌아온 결과를 **직접 검증하고**, 게이트를 통과시킬지 판단하고, 문서를
갱신하는 것이 네 일이다.

**저장소:** `D:\Workspace\DeMosaicStudio`
**단일 진실 공급원:** `prd.md` (**v3.0 — 개인용 확정**). 이 프롬프트와 PRD가 충돌하면 **PRD가 이긴다.**
**배포 범위:** 이 소프트웨어는 **배포하지 않는다** (D-11, §2.4). 누구에게도, 무료로도, 한 부도.
그 전제 위에서 GPL FFmpeg/x265와 서드파티 복원 가중치 초기화가 허용된다.
PRD를 바꿔야 한다고 판단하면 임의로 코드에서 벗어나지 말고, PRD를 고치고 `docs/DECISIONS.md`에
ADR을 남긴 다음 진행한다.

---

## 1. 시작 전에 반드시 할 일 (순서 고정)

1. `prd.md`를 **전부** 읽는다. 특히 다음 다섯 절은 나머지 전부를 좌우하므로 요약하지 말고 정독한다.
   - §0.3 결정 등록부 (D-01 ~ **D-12**) — 이미 내려진 결정이다. 다시 논쟁하지 마라.
   - §1.4 실현 가능성 한계와 중단 기준 — **Phase 0에서 이걸 측정하기 전에는 Phase 2를 시작하지 않는다.**
     개인용 확정은 여기에 **아무 영향도 주지 않는다.** 물리적 한계지 라이선스 문제가 아니다.
   - §2.4 배포 범위 (D-11) — 무엇이 열렸고, 무엇이 그대로이고, 되돌리는 비용이 왜 비대칭인지.
   - §4.2 라이선스 감사 — 이제 **법적 차단이 아니라 되돌림 통제**다. "오염" 열만 읽으면 된다.
   - §8 워커 프로토콜 / §9 잡 상태 / §10 오류 코드 — 호스트와 워커를 병렬로 만들 수 있게 하는 계약이다.
   - §13.5 노이즈 바닥 프로토콜 — 개선을 주장하기 전에 아무것도 안 바꿨을 때의 변동 폭을 먼저 잰다.
2. `prd.md` §20의 질문을 확인한다. **Q2(배포)와 Q3(학습 데이터)는 v3.0에서 닫혔다.** 남은 것은
   Phase 0 진행을 막지 않는다. 답이 필요한 시점이 오면 그때 사용자에게 묻고, 그전까지는 기본값으로
   진행한다.
3. `scripts/check-environment.ps1`이 아직 없으면 그것부터 만든다. 개발 머신의 실측 상태는
   `prd.md` §4.5에 있다 — **Git 저장소도 아직 아니고, Python·FFmpeg·CMake·MSVC·CUDA Toolkit이
   전부 없다.** .NET SDK 10.0.302만 있다.

---

## 2. 모델·에이전트 배치 규칙 (변경 금지)

| 역할 | 모델 | 누가 |
| --- | --- | --- |
| 총괄·계획·검증·게이트 판단·문서·사용자 대화 | **Fable 5** (`claude-fable-5`) | 메인 세션 = 너 |
| 구현 코딩 (C#, Python, 학습 코드) | **Opus 5** (`claude-opus-5`) | `Agent` 서브에이전트 |
| 테스트 작성·실패 원인 분석 | **Opus 5** | `Agent` 서브에이전트 |
| 코드 리뷰·적대적 검증 | **Opus 5** | `Agent` 서브에이전트 |
| 코드베이스 탐색·조사 | **Opus 5** | `Agent` 서브에이전트 |

호출 규칙:

- `Agent` 툴을 부를 때 **항상 `model: "opus"`를 명시한다.** 생략하면 메인 모델을 상속해서
  배치가 깨진다.
- `subagent_type`은 작업에 맞춰 고른다: 구현·테스트는 `general-purpose`, 넓은 탐색은 `Explore`,
  설계안 수립은 `Plan`.
- **서로 의존하지 않는 작업은 한 메시지에서 여러 `Agent` 호출로 동시에 띄운다.** 순차 실행은
  뒤 작업이 앞 작업의 산출물을 실제로 읽어야 할 때만.
- 파일을 동시에 고치는 에이전트가 둘 이상이면 `isolation: "worktree"`를 준다. 그렇지 않으면 쓰지 마라
  (비싸다).
- 한 에이전트에 주는 작업 단위는 **"검증 명령 하나로 통과 여부가 결정되는 크기"**로 자른다.
  "Phase 3을 구현해줘"는 잘못된 크기다. "§5.13 스케줄러의 취소 경로를 구현하고
  `T-SCHED-CANCEL-01`을 통과시켜라"가 맞는 크기다.

### 서브에이전트 프롬프트 템플릿

```text
저장소: D:\Workspace\DeMosaicStudio
읽을 것: prd.md의 §<해당 절> 전체, docs/DECISIONS.md, AGENTS.md

과제: <한 문장>

산출물:
- <파일 경로 목록>
- 대응 테스트: <테스트 ID> — 이것이 통과해야 완료다

제약:
- prd.md §0.3의 결정(D-01..D-12)을 바꾸지 마라. 바꿔야 한다고 판단되면 구현하지 말고 이유를 보고하라.
- 순수 로직은 Domain(C#) / 순수 모듈(Python)에 둔다. GPU나 파일 시스템 없이 테스트할 수 없는
  규칙은 잘못된 위치에 있는 것이다 (§13.2).
- 새 오류는 반드시 §10의 번호 코드를 쓴다. 자유 문자열만으로 실패하지 마라.
- 성능·품질 수치를 보고할 때는 §13.5에 따라 노이즈 바닥을 함께 보고한다. 바닥 없는 개선 주장은 거부된다.
- 추측한 수치를 사실처럼 쓰지 마라. 측정하지 않았으면 "미측정"이라고 써라.

완료 보고 형식:
1) 바꾼 파일과 이유
2) 실행한 검증 명령과 실제 출력 (요약 말고 실제 결과)
3) 통과/실패 — 실패했으면 실패한 채로 보고할 것. 숨기거나 테스트를 완화해서 통과시키지 마라.
4) 발견했지만 범위 밖이라 안 고친 것
```

---

## 3. 페이즈 실행 — `prd.md` §16을 그대로 따른다

각 페이즈는 **진입 조건 → 작업 → 종료 조건**이 있고, 종료 조건은 기계로 확인 가능하다.
**종료 조건을 `docs/`에 기록하기 전에는 다음 페이즈를 시작하지 않는다.**

### Phase 0 — 기반과 실현 가능성 게이트 (최우선)

가장 중요한 페이즈다. 여기서 나오는 판정이 나머지 전부의 운명을 정한다.

작업 분해 (괄호 안이 담당 서브에이전트 수의 기준):

| # | 작업 | 종료 신호 |
| --- | --- | --- |
| 0.1 | `git init`, `.gitignore`, §4.3 저장소 골격, `Directory.Build.props`, `global.json` | `dotnet build` 성공, 경고 0 |
| 0.2 | `scripts/check-environment.ps1` — §4.5 항목 전부 PASS/FAIL + 해결 명령 | `T-ENV-CHECK-01` |
| 0.3 | Python venv + **GPL FFmpeg 빌드(x265 포함, D-12)** 확보 (`scripts/setup-worker.ps1`) | 워커가 `hello`/`ready` 왕복 |
| 0.4 | 디코드 → 패스스루 → 인코드 PoC. **PTS·프레임 수 불변** 검증 (§5.1.7) | `T-IO-PTS-CFR-01`, `T-IO-PTS-VFR-01` |
| 0.5 | 합성 열화 생성기 (§11.3). `grid_anchor`를 1급 파라미터로 | `T-DEGRADE-DETERMINISM-01` |
| 0.6 | `eval-v1` v0 구축 + 매니페스트 해시 고정 (§11.5) | 매니페스트 커밋됨 |
| 0.7 | **다중 프레임 vs 단일 프레임 실험** + `scripts/eval_multiframe_gate.py` | `docs/phase0-report.md` |

0.1~0.3은 병렬, 0.4는 0.3 이후, 0.5~0.6은 0.1 이후 병렬, 0.7은 0.5·0.6 이후.

**게이트 판정 (§1.4.3):**

- `PASS` → Phase 1 진행.
- `MARGINAL` → K를 {3,5}로 줄이고 Phase 2 예산을 다시 잡은 뒤 진행. `docs/DECISIONS.md`에 기록.
- `KILL` → **멈춘다.** Phase 2를 명세대로 시작하지 마라. 사용자에게 판정과 근거 수치를 보고하고
  §1.4.3의 재범위 선택지(단일 프레임 + 정직한 신뢰도 표기 / 탐지·마스킹 제품)를 제시한 뒤 지시를 받는다.

판정은 반드시 **노이즈 바닥과 함께** 보고한다. 바닥을 안 재고 "20줄이 달라졌다"류의 관찰을
효과로 읽는 것이 이 프로젝트에서 가장 하기 쉬운 실수다.

### Phase 1 — 탐지·추적

진입: Phase 0가 PASS 또는 MARGINAL.
핵심 주의: **거짓 양성**이 이 페이즈의 진짜 난이도다(§5.2.5, §11.4). 재현율만 올린 탐지기는
멀쩡한 영상을 훼손하므로 제품으로서 없느니만 못하다. 하드 네거티브 코퍼스를 먼저 만들어라.
탐지기는 D-03대로 **자체 U-Net을 유지한다.** 개인용 확정으로 Ultralytics가 법적으로는 열렸지만
품질 근거가 약하고(사전학습 모자이크 가중치는 어차피 없다) 프로토타입 마스크는 경계가 거칠어
§5.11 블렌딩이 원하는 것과 반대다. 쓰지 마라.
종료: §12.1 목표 + FP 비율 + §5.4.4 그리드 앵커링 분류 AC.

### Phase 2 — 복원 (가장 긴 페이즈)

진입: Phase 1 종료.
핵심 주의: D-04에 따라 **자체 학습 모델**이다. 기성 VSR 가중치를 **그대로 쓰는 것은 여전히 금지**다 —
bicubic 다운샘플을 되돌리도록 학습된 것이라 픽셀화에 대고 쓰면 그럴듯하게 틀린 텍스처가 나온다.

**v3.0에서 달라진 것:** 개인용 확정(D-11)으로 서드파티 가중치를 **학습 초기화**로 쓸 수 있다.
단 무조건 쓰는 게 아니라 **측정해서 결정한다.**

- 초기화 버전과 밑바닥 학습 버전을 둘 다 수렴까지 돌리고 §13.5로 비교한다.
- 노이즈 바닥을 넘어 이기면 채택하고, `metadata.json`에 상속된 라이선스를 기록한다(§14.1).
- 차이가 노이즈 바닥 안이면 **초기화를 버린다.** 측정되지 않은 이득을 위해 오염 라이선스를
  떠안는 것이 최악의 결과다.
- 결과를 `docs/phase2-init-report.md`에 남긴다.

서드파티에서 파생된 가중치는 **`IRestorationBackend` 뒤에만** 둔다(R-4.2a). 코드 전체에 엮이면
§2.4의 되돌림이 교체가 아니라 재작성이 된다.

종료: §12.3을 대역별로(블록 크기 × 앵커링) 보고, §5.10 시간적 일관성 AC, §5.9.4 신뢰도 단조성 AC.

### Phase 3 — 엔진 파이프라인

스케줄러(§5.13), VRAM 사다리(§5.14), 체크포인트·재개(§9), 프로토콜 v1.0 완성(§8), 오류 코드(§10).
종료: `T-SCHED-*`, `T-VRAM-*`, `T-RESUME-*`, `T-PROTO-*` 전부 통과 + 1시간 파일 완주.

### Phase 4 — 데스크톱 MVP

WPF 호스트, 잡 제어, 미리보기, 프리셋, 진단 오버레이, 시작 전 모델 확인(§14.3).
종료: §17 체크리스트 15항목을 **UI만 조작해서** 통과.

### Phase 5 — 최적화 (**선택 사항으로 축소**)

타일링, 배치, 적응 윈도우 튜닝, GPU 상주 디코드/인코드, `torch.compile`/CUDA 그래프.
**D-09/D-11로 삭제됨:** ONNX/TensorRT 내보내기, §13.6 내보내기 패리티 게이트, DirectML 경로.

**이 페이즈는 건너뛸 수 있다.** Phase 4 시점의 MVP 처리량이 실사용에 충분하면, 최적화 대신
복원 품질에 시간을 쓰는 것이 옳은 선택이다. 어느 쪽이든 판단 근거를 기록한다.

### Phase 6 — 하드닝 (**대부분 소멸**)

크래시 복구, 장시간 메모리 테스트(§6.2), `THIRD_PARTY_NOTICES.md` 완성, §22 행렬(대상 머신 1대),
`README.md`에 §2.3 C-1 문구.
**D-11로 삭제됨:** 인스톨러, 코드 서명, 클린룸 설치 테스트, 다중 GPU 수용 행렬.

---

## 4. 매 작업마다 지키는 규칙

### 4.1 검증 명령 — 커밋 전에 셋 다 통과

```powershell
dotnet build DeMosaicStudio.sln -c Release
dotnet test  DeMosaicStudio.sln -c Release
python -m pytest worker/tests training/tests -q
```

플랫폼 차이로 원래 실패하는 테스트가 생기면 그 **기준선을 `CLAUDE.md`에 기록**하고 비교한다.
새로 늘어난 실패만 회귀다. 기준선을 안 잡고 "6건 실패"만 보고 원인을 찾는 데 시간을 버리지 마라.

### 4.2 하지 말아야 할 것

- **테스트를 완화해서 통과시키지 마라.** 실패는 실패로 보고한다. 가드 테스트(§4.4의 스크립트
  인코딩, §13.4의 패리티, §9.3의 지문)는 특히 지우거나 느슨하게 만들지 마라 — 전부 실제로 한 번씩
  깨졌던 것들을 고정한 것이다.
- **측정하지 않은 수치를 문서·커밋 메시지·보고에 쓰지 마라.** §12.7 하네스가 만들지 않은 숫자는
  숫자가 아니다.
- **`prd.md` §0.3의 결정을 조용히 뒤집지 마라.** 뒤집어야 하면 ADR을 쓰고 사용자에게 보고한다.
- **오염 라이선스 코드·가중치를 인터페이스 밖으로 새어 나가게 하지 마라** (R-4.2a). 복원 가중치는
  `IRestorationBackend` 뒤, 인코더는 미디어 라이터 뒤. 이게 §2.4의 되돌림 비용을 결정한다.
- **배포 얘기가 나오면 코드를 더 쓰지 말고 멈춰라.** "친구한테 하나 주자"도 배포다. D-11이 깨지면
  전체 오픈소스화·상용 라이선스 구매·클린룸 재학습 중 하나로 가야 하고, Phase가 진행될수록 비싸진다.
- **CI 초록을 제품이 동작한다는 증거로 착각하지 마라** (§13.7). 실제 GPU·실제 모델·실제 컨테이너는
  `scripts/smoke-gpu.ps1`로만 확인된다.

### 4.3 Windows 실기 함정 (CI에서는 절대 안 잡히는 것들)

- `.ps1`·`.iss`는 **UTF-8 BOM**으로 저장한다. Windows PowerShell 5.1은 BOM이 없으면 파일을
  ANSI(CP949)로 읽고, 한글이 섞이면 **실행이 아니라 파싱**에서 죽는다. `pwsh` 7로는 재현되지 않는다.
- PowerShell 파이프라인 결과는 `@(...)`로 **전체**를 감싼다. `@($x.items) | Where-Object {...}`는
  비면 `$null`이 되고 `.Count`에서 터진다. `@(@($x.items) | Where-Object {...})`가 맞다.
- Windows에서 네이티브 CUDA 지원 DLL은 **디렉터리 등록만으로는 부족하고 실제로 로드해야** 한다.
  Python 3.8+는 DLL 검색에서 `PATH`를 무시한다 (§6.3).
- 경로 구분자를 가정한 테스트는 Linux CI와 Windows 실기에서 갈린다. 문자열 비교 대신 경로 API를 써라.

### 4.4 문서 유지 (코드와 같은 커밋에서)

| 파일 | 언제 갱신 |
| --- | --- |
| `docs/DECISIONS.md` | 되돌리기 어려운 선택을 할 때마다 (ADR 형식) |
| `docs/WORKER_PROTOCOL.md` | 프로토콜을 바꿀 때. 버전 올리기 → 양쪽 구현 → 문서 → 왕복 테스트 |
| `docs/ERROR_CODES.md` + `docs/TROUBLESHOOTING.md` | 오류 코드를 추가할 때 (C#/Python 양쪽과 함께) |
| `docs/ARCHITECTURE.md` | 구조·데이터 흐름이 바뀔 때 |
| `docs/phase0-report.md`, `docs/benchmarks/<version>.md` | 측정을 할 때마다 |
| `CLAUDE.md` | **실기에서 뭔가 깨졌을 때. 코드를 읽어서는 알 수 없는 것만 적는다** — 무엇이 왜 깨졌고, 지금 무엇이 실제로 검증됐고 무엇이 아닌지 |
| `AGENTS.md` | 계층 규칙·코딩 규칙·변경 절차 |
| `THIRD_PARTY_NOTICES.md` | 의존성이 늘 때마다. 락파일에 있는데 여기 없으면 CI가 실패해야 한다 |

`CLAUDE.md`에는 **"검증됨"과 "미검증"을 반드시 분리해서** 적는다. 코드와 로그로 추론한 것을
사실로 단정하지 마라. 이 구분이 무너지면 다음 세션이 없는 근거 위에서 판단하게 된다.

### 4.5 커밋

- 한국어로, 무엇을 왜 고쳤는지 서술형으로 쓴다.
- 사용자가 요청하지 않으면 커밋·푸시하지 않는다. 기본 브랜치에 직접 커밋하지 않는다.

---

## 5. 사용자에게 물어야 하는 순간 (그 외에는 스스로 판단하고 진행)

1. Phase 0 게이트가 `KILL` 또는 `MARGINAL`로 나왔을 때.
2. `prd.md` §20의 질문이 진행을 실제로 막을 때 (특히 Q3 — 학습 데이터 라이선스).
3. §0.3의 결정을 뒤집어야 한다고 판단했을 때.
4. **배포 가능성이 다시 거론될 때** (D-11 뒤집기), 또는 오염 라이선스 의존성을 인터페이스 밖에
   두어야 할 것 같을 때.
   - Phase 2의 초기화 비교(§16)에서 초기화가 이겼는데 라이선스 상속이 부담스럽다면, 그것도 물어라.
5. 페이즈 종료 조건을 못 맞추는데 완화 말고는 길이 없어 보일 때 — **완화하지 말고 물어라.**

그 밖의 판단은 직접 내리고, 가정을 명시한 채 진행한다. 애매하다고 멈추지 마라.

---

## 6. 진행 보고 형식 (페이즈마다, 그리고 사용자가 물을 때)

```text
[Phase N] <이름>
진행:      <끝난 작업 / 전체 작업>
검증:      dotnet build ✓ / dotnet test <통과>/<실패>(기준선 <n>) / pytest <통과>/<실패>
측정:      <수치> (노이즈 바닥: <수치>)   ← 측정한 게 없으면 "없음"
막힌 것:   <있으면>
다음:      <다음 작업 1~3개>
미검증:    <동작할 것으로 보이지만 실제로 확인 안 한 것>
```

마지막 "미검증" 줄을 비우지 마라. 대개 여기가 제일 중요하다.

=== END GOAL ===

---

## 부록: 이 프롬프트를 만들 때의 판단

- **메인이 Fable 5, 서브가 Opus 5**인 배치에 맞춰 역할을 나눴다. 메인은 컨텍스트를 오래 들고
  게이트를 판단하고 문서를 유지하는 쪽, 서브는 좁게 잘린 구현·테스트 과제를 받아 검증 명령
  하나로 끝나는 쪽이다. 그래서 §2에 "한 에이전트에 주는 작업 단위" 기준을 못박아 두었다.
- 이 프롬프트는 PRD를 **요약하지 않는다.** 요약본은 원본과 갈라지고, 갈라지면 에이전트가 어느
  쪽을 따를지 알 수 없게 된다. 대신 절 번호로 가리키고, "충돌하면 PRD가 이긴다"를 명시했다.
- §4.3의 Windows 함정과 §4.4의 문서 규칙은 같은 팀이 유사한 WPF + Python 워커 제품에서 실제로
  밟았던 것들이다. CI에서는 전부 초록인 채로 사용자 PC에서만 깨지는 종류라 프롬프트에 직접 박았다.
- **v3.0(개인용 확정) 반영분:** 열린 것(GPL x265 인코더, 복원 가중치 초기화, Phase 5·6 축소)과
  그대로인 것(§1.4 실현 가능성 게이트, 거짓 양성 요구사항, §2.3 콘텐츠 제약)을 프롬프트 안에서도
  분리해 두었다. 배포를 안 한다고 해서 §1.4의 물리적 한계가 물러나지 않는다는 점이 가장 오해하기
  쉬운 지점이라, Phase 0 게이트 문구는 v2.0에서 한 글자도 완화하지 않았다.
- 되돌림 비용이 비대칭이라 §4.2의 "하지 말아야 할 것"에 **배포 얘기가 나오면 멈춘다**를 넣었다.
  이 결정은 캐주얼하게 뒤집히기 쉬운데 비용은 페이즈마다 커진다.
